  Has been sent along with java files and a file named readme.txt
	
     *   A SQL.sql file which contains the code, which JLOCATION,JCONNECTION table has been created  in sqlDeveloper
     *   A cities.dat file which contains all the location objects.
	   This file is ready for use or ,if you would like, you could create your own.
     *   A directlinks.dat file which contains only the direct connection objects. 
	   This file is ready for use or ,if you would like, you could create your own(it takes many hours to complete).
  IMPORTANT!!  
     * A Login.encrypted  file which contains the login credentials with a particular encryption for the database connection.

	We inform that the database has already data inside and if an update is required it will need a reset first.